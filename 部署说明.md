# TapTap 登录服务端部署说明

## 📋 项目结构

```
server/
├── package.json      # 项目依赖配置
├── server.js         # 服务端主程序
└── 部署说明.md       # 本说明文档
```

## 🚀 部署方式

### 方式一：本地测试（免费）

#### 1. 安装 Node.js
- 访问 https://nodejs.org/
- 下载 LTS 版本并安装

#### 2. 安装依赖
```bash
cd server
npm install
```

#### 3. 启动服务
```bash
npm start
```

服务将运行在 http://localhost:3000

#### 4. 测试
打开浏览器访问：
- http://localhost:3000/health
- 应该返回 `{"status":"ok"}`

---

### 方式二：免费云平台部署（推荐）

#### 选项 A：Vercel（免费，最简单）

1. 注册 Vercel 账号：https://vercel.com
2. 安装 Vercel CLI：
   ```bash
   npm i -g vercel
   ```
3. 部署：
   ```bash
   cd server
   vercel
   ```
4. 按提示完成部署

#### 选项 B：Railway（免费）

1. 注册 Railway 账号：https://railway.app
2. 连接 GitHub 仓库
3. 自动部署

#### 选项 C：Render（免费）

1. 注册 Render 账号：https://render.com
2. 创建 Web Service
3. 选择 Node.js 环境
4. 部署

---

### 方式三：自有服务器（如果有）

#### 使用 PM2 守护进程

```bash
# 安装 PM2
npm install -g pm2

# 启动服务
pm2 start server.js --name "xianxia-auth"

# 保存配置
pm2 save

# 设置开机自启
pm2 startup
```

---

## ⚙️ 配置修改

### 1. 修改服务端配置

编辑 `server/server.js`：

```javascript
const TAP_CONFIG = {
    clientId: 'csg5qlajcgr157ix01',
    clientToken: 'bvDH35Gw2SCcgkmNFrmQxLi0xaMylxLXEB6VNMUK',
    redirectUri: 'https://你的域名/callback', // 修改这里
    // ...
};
```

### 2. 修改前端配置

编辑 `模拟宗门/taptap-web-login.js`：

```javascript
const TapTapWebLogin = {
    serverUrl: 'https://你的域名', // 修改这里
    // ...
};
```

### 3. 修改 TapTap 开发者中心配置

1. 登录 https://developer.taptap.cn/
2. 进入应用配置
3. 修改 **OAuth 回调地址** 为：
   ```
   https://你的域名/taptap-callback.html
   ```

---

## 📝 部署步骤总结

### 第一步：部署服务端

选择上面的一种部署方式，将服务端部署到云端。

### 第二步：获取域名

部署成功后会获得一个域名，例如：
- `https://xianxia-auth.vercel.app`
- `https://xianxia-auth.railway.app`

### 第三步：修改配置

1. 修改服务端 `redirectUri`
2. 修改前端 `serverUrl`
3. 修改 TapTap 开发者中心回调地址

### 第四步：重新打包游戏

在 HBuilderX 中重新打包 APK。

### 第五步：测试

1. 安装 APK
2. 点击 TapTap 登录
3. 应该能正常跳转到 TapTap 授权页面
4. 授权后返回游戏，完成登录

---

## 🔧 常见问题

### Q: 部署后无法访问？

A: 检查：
1. 服务是否正常启动
2. 防火墙是否放行端口
3. 域名解析是否正确

### Q: 登录时提示 "网络错误"？

A: 检查：
1. 前端 `serverUrl` 配置是否正确
2. 服务端是否运行
3. 手机网络是否正常

### Q: TapTap 授权后无法返回游戏？

A: 检查：
1. `redirectUri` 配置是否正确
2. 回调页面 `taptap-callback.html` 是否存在
3. 域名是否一致

### Q: 提示 "client_id 无效"？

A: 检查：
1. TapTap 开发者中心配置的包名是否正确
2. Client ID 是否正确

---

## 💡 推荐方案

**没有服务器的情况下，推荐使用 Vercel**：
- 完全免费
- 部署简单（一条命令）
- 自动 HTTPS
- 国内访问速度尚可

**部署命令**：
```bash
cd server
npm i -g vercel
vercel
```

按提示操作即可，5 分钟完成部署！
