# TapTap 登录服务端部署方案

由于 Glitch 已停止服务，这里提供其他免费部署方案。

## 🚀 方案一：Railway（推荐）

### 特点
- ✅ 免费额度充足（每月 500 小时运行时间）
- ✅ 支持 Node.js
- ✅ 自动 HTTPS
- ✅ 国内访问速度较好

### 部署步骤

1. **注册账号**
   - 访问 https://railway.app
   - 用 GitHub 账号登录

2. **创建项目**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"

3. **上传代码到 GitHub**
   ```bash
   # 在 server 文件夹中初始化 git
   cd server
   git init
   git add .
   git commit -m "Initial commit"
   
   # 创建 GitHub 仓库并推送
   git remote add origin https://github.com/你的用户名/xianxia-auth-server.git
   git push -u origin main
   ```

4. **部署**
   - 在 Railway 中选择你的仓库
   - 自动部署完成
   - 获取域名（如 `https://xianxia-auth.up.railway.app`）

---

## 🚀 方案二：Render

### 特点
- ✅ 完全免费
- ✅ 支持 Node.js
- ✅ 自动 HTTPS
- ⚠️ 免费版有休眠机制（15分钟无访问会休眠）

### 部署步骤

1. **注册账号**
   - 访问 https://render.com
   - 用 GitHub 账号登录

2. **创建 Web Service**
   - 点击 "New" → "Web Service"
   - 连接 GitHub 仓库

3. **配置**
   - Build Command: `npm install`
   - Start Command: `npm start`
   - 点击 "Create Web Service"

4. **获取域名**
   - 部署完成后获得域名（如 `https://xianxia-auth.onrender.com`）

---

## 🚀 方案三：Fly.io

### 特点
- ✅ 免费额度充足
- ✅ 支持 Docker 和 Node.js
- ✅ 全球 CDN
- ⚠️ 需要信用卡验证（不扣费）

### 部署步骤

1. **安装 Fly CLI**
   ```bash
   # Windows
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **登录**
   ```bash
   fly auth login
   ```

3. **部署**
   ```bash
   cd server
   fly launch
   fly deploy
   ```

---

## 🚀 方案四：本地电脑 + 内网穿透（临时测试）

### 适合场景
- 快速测试
- 没有服务器
- 临时使用

### 步骤

1. **启动本地服务**
   ```bash
   cd server
   npm install
   npm start
   ```

2. **安装内网穿透工具（推荐 ngrok）**
   - 访问 https://ngrok.com
   - 注册账号，获取 authtoken
   - 下载并安装

3. **启动内网穿透**
   ```bash
   ngrok http 3000
   ```

4. **获取公网域名**
   - ngrok 会分配一个临时域名（如 `https://abc123.ngrok.io`）
   - 有效期 8 小时（免费版）

---

## 🚀 方案五：Vercel（需修改代码）

Vercel 主要用于前端，但也可以部署简单的 Node.js API。

### 限制
- 无状态（不能保存文件）
- 适合无状态服务
- 免费额度充足

### 部署步骤

1. **修改代码**
   创建 `api/login.js`：
   ```javascript
   // 将 server.js 改为 Vercel Serverless 格式
   module.exports = async (req, res) => {
     // 登录逻辑
   };
   ```

2. **部署**
   - 访问 https://vercel.com
   - 导入 GitHub 仓库
   - 自动部署

---

## 💡 推荐方案

| 方案 | 难度 | 稳定性 | 免费额度 | 推荐度 |
|------|------|--------|----------|--------|
| **Railway** | ⭐⭐ | ⭐⭐⭐⭐⭐ | 充足 | ⭐⭐⭐⭐⭐ |
| **Render** | ⭐⭐ | ⭐⭐⭐⭐ | 充足 | ⭐⭐⭐⭐ |
| **Fly.io** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 充足 | ⭐⭐⭐⭐ |
| **ngrok** | ⭐ | ⭐⭐ | 临时 | ⭐⭐ |

---

## 📝 部署后配置

无论选择哪种方案，部署后都需要：

1. **修改服务端配置**
   ```javascript
   // server.js
   const TAP_CONFIG = {
       redirectUri: 'https://你的实际域名/taptap-callback.html',
       // ...
   };
   ```

2. **修改前端配置**
   ```javascript
   // taptap-web-login.js
   serverUrl: 'https://你的实际域名'
   ```

3. **配置 TapTap 开发者中心**
   - OAuth 回调地址：`https://你的实际域名/taptap-callback.html`

---

## ⚠️ 重要提醒

1. **Client Token 安全**
   - 不要提交到公共仓库
   - 使用环境变量存储
   - 在 Railway/Render 中设置环境变量

2. **域名变更**
   - 免费平台的域名可能会变
   - 需要同步更新 TapTap 配置

3. **HTTPS 必需**
   - TapTap OAuth 要求 HTTPS
   - 所有平台都自动提供 HTTPS
